// Server-Komponente: rendert ein JSON-LD-Script für strukturierte Daten (SEO).
export function JsonLd({ data }: { data: object }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
